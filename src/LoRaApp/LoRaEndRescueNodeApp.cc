/*
 * LoRaEndRescueNodeApp.cc
 * A thin wrapper around LoRaEndNodeApp allowing separate NED type (endRescueNode).
 */
#include "LoRaEndRescueNodeApp.h"

namespace inet {

Define_Module(LoRaEndRescueNodeApp);

} // namespace inet
